def field(n):
    rtr = ['_'] * n
    for r in range(n):
        rtr = ['_'] * n
        if r == 0 or r == n-1:
            rtr = ['Z'] * n
        else:
            rtr[n-r-1] = 'Z'
            rtr[n-r] = '_'
        print(rtr)

field(7)
