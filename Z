def field(n):
    rtr = ['_'] * n
    for r in range(n):
        rtr = ['_'] * n
        if r == 0 or r == n-1:
            rtr = ['X'] * n
        else:
            rtr[n-r-1] = 'X'
            rtr[n-r] = '_'
        print(rtr)

field(7)
